extends layout

block content
  div
    table.mainTable
      thead
        tr
          each headerItem in ["FirstName", "LastName", "Email", "Age"]
            th(class=headerItem.toLowerCase())= headerItem
      tbody
        each user in locals.usersList
          tr
            td= user.firstName
            td= user.lastName
            td= user.email
            td= user.age
